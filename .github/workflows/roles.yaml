name: ROLES DEPLOY OF NGINX
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ansible-deploy:
    runs-on: ansible-master

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Ansible Role
        run: |
          ansible-galaxy install -p /home/user20/ansible/roles   geerlingguy.nginx
      
      - name: Run Ansible playbook
        env:
          ANSIBLE_HOST_KEY_CHECKING: False
        run: |
          pwd
          cp /home/user20/ansible/inventory.yaml  /home/user20/ansible/roles/inventory.yaml 
          cp /home/user20/ansible/ansible.cfg  /home/user20/ansible/roles/ansible.cfg 
          ansible-playbook  ./ansible/site.yaml -i /home/user20/ansible/inventory.yaml  --become
